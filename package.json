{
  "name": "monorepo-experiment",
  "version": "1.0.0",
  "description": "Testing a monorepo project arc",
  "main": "index.js",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2",
    "@monorepo-experiment/eslint-config": "1.0.0"
  }
}
